# Tests

Project-level integration and fixture tests will live here as the scanner, proxy, policy, and audit behavior grows.

Package-local unit tests can live beside source files under `packages/*/src`.
