# MCP ToolLatch v0.3.0-beta.1 Gitee / GitHub 同步报告

## 结论

MCP ToolLatch 已完成 Gitee 与 GitHub 双平台同步。`master` 分支已推送到 GitHub，`v0.3.0-beta.1` tag 已推送到 GitHub；Gitee 端 `master` 与 `v0.3.0-beta.1` tag 确认为已同步。

当前可以把 GitHub 仓库链接发给开发者和安全研究者做小范围 beta 试用。

## 仓库状态

- 验收日期：2026-06-05
- 当前分支：`master`
- 当前 commit：`27a3b32eb0aec8eff5627fe73e937d9b6c01624c`
- 当前最新提交说明：`docs: add beta trial feedback templates`
- 当前 tag：`v0.3.0-beta.1`
- tag 目标提交：`0def0a47f3a7e3ea955e14c4d4d4de210999087f`
- Gitee remote：`git@gitee.com:guntherm/mcp-toollatch.git`
- GitHub remote：`git@github.com:Gunther-M/mcp-toollatch.git`

## 同步结果

- GitHub SSH 验证：通过。`ssh -T git@github.com` 返回已认证且 GitHub 不提供 shell access。
- Gitee pull：通过。`git pull --rebase --autostash origin master` 返回 `Already up to date.`
- GitHub master push：通过。`git push github master` 已创建/更新 GitHub `master` 分支。
- GitHub tag push：通过。`git push github v0.3.0-beta.1` 已推送 `v0.3.0-beta.1` tag。
- Gitee master push：通过。`git push origin master` 返回 `Everything up-to-date`。
- Gitee tag push：通过。`git push origin v0.3.0-beta.1` 返回 `Everything up-to-date`。

## Release 状态

- GitHub Release 是否已创建：待手动创建。
- Gitee Release 是否已创建：待手动创建。

手动创建 Release 步骤：

1. 打开 GitHub/Gitee Releases。
2. 选择 tag `v0.3.0-beta.1`。
3. title 填 `MCP ToolLatch v0.3.0-beta.1`。
4. 内容复制 `docs/releases/v0.3.0-beta.1.md`。
5. 勾选 prerelease / beta。
6. 发布 Release。

## 试用建议

建议小范围外部试用。适合发给 MCP 用户、Cursor / Claude Desktop / VS Code 用户、MCP server 开发者、AI Agent 工具链开发者和安全研究者进行本地验证。

## 安全边界

MCP ToolLatch v0.3.0-beta.1 是 beta 阶段的本地 policy / approval / audit gateway。它不是生产级完整沙箱，不提供内核级隔离，也不承诺防御所有 prompt injection 或所有恶意工具行为。试用反馈应避免公开提交真实 `.env`、token、SSH 私钥、证书、cookie 或未脱敏日志。
